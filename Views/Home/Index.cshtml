@{
    ViewData["Title"] = "หน้าแรก";
}

<div class="container text-center mt-5">
    <!-- ส่วนหัว -->
    <h1 class="display-4 font-weight-bold text-primary mb-3">
        ChillVibesCrew
    </h1>

    <!-- ข้อความรอง -->
    <p class="lead text-muted mb-4">
        ระบบจัดการสินค้าคงคลังและอีเมล
    </p>

    <!-- ปุ่มเรียกร้องให้ทำการ -->
    
</div>

<!-- รวมไอคอนของ Bootstrap สำหรับปุ่ม -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

<!-- การตกแต่ง -->
<style>
    /* เพิ่มช่องว่างด้านบนของหน้า */
    .container {
        margin-top: 100px;
    }

    /* การตกแต่งส่วนหัว */
    h1 {
        font-size: 4rem;
        color: #007bff;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 2px;
    }

    /* การตกแต่งข้อความรอง */
    .lead {
        font-size: 1.25rem;
        color: #6c757d;
        font-weight: 300;
    }

    /* การตกแต่งข้อความคำอธิบาย */
    .text-dark {
        font-size: 1.2rem;
        color: #343a40;
        font-weight: 400;
    }

    /* การตกแต่งปุ่ม */
    .btn-lg {
        font-size: 18px;
        padding: 12px 30px;
        border-radius: 50px;
    }

    .btn-success {
        background-color: #28a745;
        border-color: #28a745;
        font-weight: bold;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .btn-success i {
            margin-right: 8px;
        }
</style>
